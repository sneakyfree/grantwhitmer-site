# Always revalidate HTML/CSS/JS so deploys show up immediately (no stale cache).
/*
  Cache-Control: public, max-age=0, must-revalidate

# Fingerprinted/static media can cache long.
/assets/*
  Cache-Control: public, max-age=2592000
